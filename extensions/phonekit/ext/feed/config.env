# ===== PhoneKit Feed Reader configuration =====
# Feed list lives in feeds.txt (one URL per line); see its header for sources.
# The server fetches each feed and caches items in cache.json beside it.
PK_FEED_PORT="8082"